<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> {{ title }} - Durham's Veg</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
  </head>

  <body>
    <h1>Miss Durham's Veg</h1>
    <h3>Order List</h3>
    {%for order in orders%}
    <li><a href="{{url_for('order', order_index=order.index)}}">Order {{order.index}}: {{order.type}} - Quantity:
        {{order.quantity}}</a></li>
      {# url_for('order', order_index=order.index) 'order' is the name of the function#}
    {%endfor%}
  </body>

</html>